return {
    current = require("viml.movement.tag.current"),
    id       = require("viml.movement.tag.id"),
    next     = require("viml.movement.tag.next"),
    previous = require("viml.movement.tag.previous"),
}
